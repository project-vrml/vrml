package group.rxcloud.vrml.stopwatch.metric;

import group.rxcloud.vrml.metric.index.MetricIndex;

/**
 * Basic metric for StopWatch
 */
public enum StopWatchMetric implements MetricIndex {

    ID,
    TaskName,
    RunningTime,
}
